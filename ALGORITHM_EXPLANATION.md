# Объяснение алгоритма планирования задач PlanBot

## 📋 Общая информация

**Название алгоритма**: Deadline-Aware Greedy Scheduling (Жадный алгоритм планирования с учётом дедлайнов)

**Назначение**: Автоматическое распределение задач пользователя по дням с учётом приоритетов, дедлайнов и индивидуальных настроек рабочего времени.

**Тип алгоритма**: Жадный алгоритм (Greedy Algorithm) с эвристиками

---

## 🎯 Постановка задачи

### Входные данные:
1. **Список задач**, где каждая задача содержит:
   - `title` - название задачи
   - `hours_required` - количество часов, необходимых для выполнения
   - `priority` - приоритет (целое число от 1 до 10, где 10 - наивысший)
   - `deadline` - крайний срок выполнения (может отсутствовать)

2. **Настройки пользователя**:
   - `daily_capacity` - сколько часов в день пользователь может работать (по умолчанию 8)
   - `work_days` - массив рабочих дней недели (1=Пн, 2=Вт, ..., 7=Вс)
   - Пример: `[1,2,3,4,5]` означает работу с понедельника по пятницу

3. **Дата начала планирования** - с какого дня начинать распределение задач

### Выходные данные:
Расписание, где для каждого дня указано:
- Дата
- Список задач, запланированных на этот день
- Количество часов, выделенных каждой задаче
- Общая загрузка дня
- Оставшееся свободное время

### Ограничения:
- Задача может быть разбита на несколько дней
- Нельзя превышать `daily_capacity` в любой день
- Задачи с дедлайном должны быть завершены до указанной даты
- Планирование происходит только на рабочие дни

---

## 🔄 Алгоритм работы

### Этап 1: Фильтрация и подготовка данных

```
ВХОД: Все задачи пользователя
ВЫХОД: Только задачи со статусом "pending" (ожидающие планирования)

Псевдокод:
pending_tasks = []
for each task in all_tasks:
    if task.status == "pending":
        pending_tasks.append(task)
```

**Зачем**: Исключаем уже запланированные, выполненные или отменённые задачи.

---

### Этап 2: Сортировка задач по приоритету

Задачи сортируются по следующим критериям (в порядке убывания важности):

#### Критерий 1: Наличие дедлайна
```
Задачи С дедлайном > Задачи БЕЗ дедлайна
```

#### Критерий 2: Близость дедлайна (для задач с дедлайном)
```
Более ранний дедлайн > Более поздний дедлайн
```

#### Критерий 3: Приоритет (при равных дедлайнах)
```
Более высокий приоритет > Более низкий приоритет
```

#### Критерий 4: Длительность (при равных приоритетах)
```
Меньше часов > Больше часов
```

**Обоснование**: 
- Задачи с дедлайнами более критичны
- Близкие дедлайны требуют немедленного внимания
- Приоритет отражает важность задачи
- Короткие задачи быстрее освобождают время для других

**Пример сортировки**:

| № | Задача | Дедлайн | Приоритет | Часы | Порядок |
|---|--------|---------|-----------|------|---------|
| 1 | Отчёт | 10.01.2026 | 5 | 4 | **1** (ближайший дедлайн) |
| 2 | Презентация | 12.01.2026 | 8 | 6 | **2** (дедлайн позже) |
| 3 | Код-ревью | нет | 10 | 2 | **3** (нет дедлайна, но высокий приоритет) |
| 4 | Рефакторинг | нет | 10 | 5 | **4** (нет дедлайна, больше часов) |

---

### Этап 3: Распределение задач по дням

Для каждой задачи из отсортированного списка выполняется алгоритм распределения.

#### Шаг 3.1: Проверка возможности выполнения (для задач с дедлайном)

```
ФУНКЦИЯ calculateLatestStartDate(deadline, hours_required):
    // Вычисляем минимальное количество рабочих дней
    work_days_needed = CEIL(hours_required / daily_capacity)
    
    // Идём назад от дедлайна, считая только рабочие дни
    date = deadline
    work_days_found = 0
    
    while work_days_found < work_days_needed:
        date = date - 1 день
        if isWorkDay(date):
            work_days_found++
    
    return date  // Последняя возможная дата начала
```

**Пример**:
```
Дедлайн: 15.01.2026 (среда)
Требуется: 16 часов
Дневная нагрузка: 8 часов/день
Рабочие дни: Пн-Пт

Расчёт:
work_days_needed = CEIL(16 / 8) = 2 дня

Идём назад от 15.01:
- 14.01 (вторник) - рабочий день → work_days_found = 1
- 13.01 (понедельник) - рабочий день → work_days_found = 2

latest_start_date = 13.01.2026
```

**Проверка**:
```
if latest_start_date < current_date:
    ОШИБКА: "Невозможно уложиться в дедлайн"
    return false
```

---

#### Шаг 3.2: Поиск свободных слотов и распределение часов

```
ФУНКЦИЯ scheduleTask(task, start_date, day_slots):
    remaining_hours = task.hours_required
    current_date = start_date
    max_days_to_check = 365  // Не смотрим дальше года
    
    while remaining_hours > 0 AND days_checked < max_days_to_check:
        // Пропускаем нерабочие дни
        if NOT isWorkDay(current_date):
            current_date = current_date + 1 день
            continue
        
        // Проверяем дедлайн
        if task.deadline EXISTS AND current_date > task.deadline:
            return false  // Не успеваем
        
        // Вычисляем доступное время в этот день
        day_slot = getDaySlot(current_date, day_slots)
        available_hours = daily_capacity - day_slot.total_hours
        
        if available_hours > 0:
            // Выделяем максимально возможное количество часов
            hours_to_allocate = MIN(remaining_hours, available_hours)
            
            // Добавляем задачу в расписание этого дня
            day_slot.tasks.append({
                task_id: task.id,
                title: task.title,
                hours_allocated: hours_to_allocate,
                priority: task.priority,
                deadline: task.deadline
            })
            
            day_slot.total_hours += hours_to_allocate
            remaining_hours -= hours_to_allocate
        
        current_date = current_date + 1 день
    
    return remaining_hours == 0  // true если всё распределено
```

**Жадная стратегия**: На каждом шаге алгоритм выделяет задаче максимально возможное количество часов в текущем дне.

---

### Этап 4: Формирование результата

После обработки всех задач:
1. Преобразуем хэш-таблицу дней в отсортированный массив
2. Сортируем дни по возрастанию даты
3. Формируем список незапланированных задач (если есть)

```
ФУНКЦИЯ convertDaySlotsToSlice(day_slots):
    schedules = []
    for each date, slot in day_slots:
        schedules.append(slot)
    
    // Сортируем по дате
    schedules.sort(by: date ascending)
    
    return schedules
```

---

## 📊 Анализ сложности

### Временная сложность: **O(n × d)**

Где:
- `n` - количество задач
- `d` - количество дней для планирования (в худшем случае 365)

**Обоснование**:
- Сортировка задач: O(n log n)
- Для каждой задачи (n):
  - Проходим по дням (d) в поиске свободных слотов
  - Операции с хэш-таблицей: O(1)
- Итого: O(n log n) + O(n × d) = **O(n × d)** (доминирующий член)

### Пространственная сложность: **O(n + d)**

Где:
- `n` - для хранения задач
- `d` - для хранения расписания дней

**Обоснование**:
- Массив задач: O(n)
- Хэш-таблица дней: O(d)
- Результирующее расписание: O(n + d)

---

## 🧪 Примеры работы

### Пример 1: Простое планирование без конфликтов

**Входные данные**:
```
Задачи:
1. "Написать отчёт" - 4 часа, приоритет 8, дедлайн: 10.01.2026
2. "Код-ревью" - 2 часа, приоритет 5, нет дедлайна

Настройки:
- daily_capacity = 8 часов
- work_days = [1,2,3,4,5] (Пн-Пт)
- start_date = 08.01.2026 (среда)
```

**Процесс**:

1. Сортировка:
   - Задача 1 (есть дедлайн) → первая
   - Задача 2 (нет дедлайна) → вторая

2. Распределение задачи 1 "Написать отчёт":
   - 08.01 (среда) - рабочий день, доступно 8 часов
   - Выделяем 4 часа
   - Задача полностью распределена

3. Распределение задачи 2 "Код-ревью":
   - 08.01 (среда) - доступно 4 часа (8 - 4 = 4)
   - Выделяем 2 часа
   - Задача полностью распределена

**Результат**:
```
08.01.2026 (Среда): 6/8 часов
  ├─ Написать отчёт: 4 часа [дедлайн: 10.01]
  └─ Код-ревью: 2 часа
  
Свободно: 2 часа
```

---

### Пример 2: Разбиение задачи на несколько дней

**Входные данные**:
```
Задача: "Изучить фреймворк" - 20 часов, приоритет 7, нет дедлайна

Настройки:
- daily_capacity = 6 часов
- work_days = [1,2,3,4,5] (Пн-Пт)
- start_date = 13.01.2026 (понедельник)
```

**Процесс**:

1. День 1 (13.01, Пн):
   - Доступно: 6 часов
   - Выделяем: 6 часов
   - Остаток: 20 - 6 = 14 часов

2. День 2 (14.01, Вт):
   - Доступно: 6 часов
   - Выделяем: 6 часов
   - Остаток: 14 - 6 = 8 часов

3. День 3 (15.01, Ср):
   - Доступно: 6 часов
   - Выделяем: 6 часов
   - Остаток: 8 - 6 = 2 часа

4. День 4 (16.01, Чт):
   - Доступно: 6 часов
   - Выделяем: 2 часа
   - Остаток: 0 часов ✓

**Результат**:
```
13.01.2026 (Пн): 6/6 часов
  └─ Изучить фреймворк: 6 часов

14.01.2026 (Вт): 6/6 часов
  └─ Изучить фреймворк: 6 часов

15.01.2026 (Ср): 6/6 часов
  └─ Изучить фреймворк: 6 часов

16.01.2026 (Чт): 2/6 часов
  └─ Изучить фреймворк: 2 часа
```

---

### Пример 3: Невозможность выполнения в срок

**Входные данные**:
```
Задача: "Большой проект" - 40 часов, дедлайн: 09.01.2026 (завтра)

Настройки:
- daily_capacity = 8 часов
- work_days = [1,2,3,4,5]
- start_date = 08.01.2026 (среда)
```

**Процесс**:

1. Вычисление latest_start_date:
   ```
   work_days_needed = CEIL(40 / 8) = 5 дней
   
   Идём назад от 09.01:
   - 08.01 (Ср) - рабочий → 1
   - 07.01 (Вт) - рабочий → 2
   - 06.01 (Пн) - рабочий → 3
   - 03.01 (Пт) - рабочий → 4
   - 02.01 (Чт) - рабочий → 5
   
   latest_start_date = 02.01.2026
   ```

2. Проверка:
   ```
   latest_start_date (02.01) < current_date (08.01)
   ```

**Результат**: ❌ Ошибка - невозможно уложиться в дедлайн

**Объяснение**: Для выполнения 40 часов работы требуется 5 рабочих дней, но до дедлайна остался только 1 рабочий день.

---

### Пример 4: Учёт выходных дней

**Входные данные**:
```
Задача: "Подготовка презентации" - 12 часов, дедлайн: 13.01.2026

Настройки:
- daily_capacity = 8 часов
- work_days = [1,2,3,4,5] (Пн-Пт, без выходных)
- start_date = 10.01.2026 (пятница)
```

**Процесс**:

1. День 1 (10.01, Пт):
   - Рабочий день ✓
   - Выделяем: 8 часов
   - Остаток: 4 часа

2. День 2 (11.01, Сб):
   - Не рабочий день ✗
   - Пропускаем

3. День 3 (12.01, Вс):
   - Не рабочий день ✗
   - Пропускаем

4. День 4 (13.01, Пн):
   - Рабочий день ✓
   - Это день дедлайна
   - Выделяем: 4 часа
   - Остаток: 0 часов ✓

**Результат**:
```
10.01.2026 (Пт): 8/8 часов
  └─ Подготовка презентации: 8 часов [дедлайн: 13.01]

13.01.2026 (Пн): 4/8 часов
  └─ Подготовка презентации: 4 часа [дедлайн: сегодня]
```

---

## ✅ Преимущества алгоритма

1. **Гарантия соблюдения дедлайнов** (если это физически возможно)
   - Проверка выполнимости перед планированием
   - Приоритет задачам с близкими дедлайнами

2. **Гибкость настроек**
   - Индивидуальная дневная нагрузка
   - Настраиваемые рабочие дни
   - Учёт выходных и праздников

3. **Автоматическое разбиение задач**
   - Большие задачи распределяются на несколько дней
   - Равномерная загрузка

4. **Эффективность**
   - Линейная сложность по количеству задач
   - Быстрая работа даже с большим количеством задач

5. **Простота реализации**
   - Понятная логика
   - Легко отлаживать и модифицировать

---

## ⚠️ Ограничения и недостатки

### 1. Жадная стратегия не всегда оптимальна

**Проблема**: Алгоритм может создать неоптимальное расписание в некоторых случаях.

**Пример**:
```
Задачи:
A: 6 часов, дедлайн через 1 день, приоритет 5
B: 4 часа, дедлайн через 2 дня, приоритет 10

Daily capacity: 8 часов

Жадный алгоритм:
День 1: A (6 часов) + B (2 часа) = 8 часов
День 2: B (2 часа) = 2 часа

Оптимальное решение:
День 1: B (4 часа) + A (4 часа) = 8 часов
День 2: A (2 часа) = 2 часа
```

**Почему это происходит**: Жадный алгоритм выбирает локально оптимальное решение на каждом шаге, не учитывая глобальную картину.

### 2. Отсутствие учёта зависимостей между задачами

Алгоритм не поддерживает ситуации типа "задача B может начаться только после завершения задачи A".

### 3. Не учитывается контекстное переключение

При разбиении задачи на несколько дней не учитывается время на "вход в контекст" каждый день.

### 4. Отсутствие оптимизации по группировке

Алгоритм не пытается группировать похожие задачи в один день для повышения эффективности.

---

## 🔮 Возможные улучшения

### 1. Динамическое программирование
Вместо жадного подхода использовать DP для нахождения глобально оптимального решения.

### 2. Учёт зависимостей
Добавить поддержку DAG (направленного ациклического графа) зависимостей между задачами.

### 3. Машинное обучение
Предсказывать реальное время выполнения задач на основе истории пользователя.

### 4. Оптимизация по контексту
Группировать похожие задачи (по тегам, категориям) в один день.

### 5. Буферное время
Автоматически добавлять 10-20% буфера для непредвиденных обстоятельств.

---

## 📚 Связь с теорией алгоритмов

### Класс задачи
Задача планирования относится к классу **NP-трудных задач** (Job Scheduling Problem).

### Аналоги
- **Earliest Deadline First (EDF)** - алгоритм реального времени
- **Shortest Job First (SJF)** - минимизация среднего времени ожидания
- **Bin Packing** - упаковка предметов в контейнеры

### Отличия нашего алгоритма
- Комбинирует EDF (приоритет дедлайнам) и SJF (короткие задачи при равных приоритетах)
- Добавляет гибкость через пользовательские приоритеты
- Учитывает индивидуальные настройки рабочего времени

---

## 🎓 Заключение

Представленный алгоритм является практичным решением задачи планирования задач для индивидуального пользователя. Он не претендует на глобальную оптимальность, но обеспечивает:

✅ Быструю работу (O(n × d))  
✅ Соблюдение дедлайнов  
✅ Учёт приоритетов  
✅ Гибкость настроек  
✅ Простоту реализации и понимания  

Для большинства реальных сценариев использования этот алгоритм даёт достаточно хорошие результаты, а его жадная природа позволяет быстро реагировать на изменения в списке задач.

